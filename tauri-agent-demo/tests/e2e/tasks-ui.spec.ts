import { test } from '@playwright/test';

test.describe('Task Workbench', () => {
  test.skip('create -> execute -> handoff -> complete', async () => {
    // Requires running frontend/backend in CI environment.
  });
});
